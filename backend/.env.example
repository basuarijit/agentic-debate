# Backend environment placeholder.

# Use "mock" for local API testing without an OpenAI key.
# Use "openai" to call OpenAI through LangChain.
DEBATE_LLM_PROVIDER=mock

# Required only when DEBATE_LLM_PROVIDER=openai.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.4

